spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create

  task:
    scheduling:
      enabled: false


external:
  naver:
    client-id: test
    client-secret: test
    base-url: test

  api-football:
    api-key: test
    api-base-uri: test

retry:
  team-statistics:
    max-attempts: 3
    delay: 0
